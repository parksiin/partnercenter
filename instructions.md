# 🛑 프로젝트 개발 불변의 규칙 (Immutable Rules)

이 문서는 본 프로젝트의 코드를 작성할 때 반드시 따라야 하는 최우선 지침입니다. 어떠한 경우에도 임의의 수치나 테일윈드 기본 클래스를 사용하지 않습니다.

## 1. Core Principles (핵심 원칙)
- **Zero Arbitrary Values**: 절대 임의의 수치(예: `px-[13px]`, `mt-[7px]`)를 사용하지 않습니다.
- **Token Only**: 오직 `theme.css`와 `tailwind.config.js`에 정의된 토큰 클래스만 사용합니다.
- **Strict Compliance**: 테일윈드 기본 규격(예: `p-4` = 16px)을 뇌내 필터링 없이 그대로 쓰지 마십시오. 본 프로젝트에서 `p-4`는 `var(--padding-4)` (10px)로 매핑되어 있습니다.

## 2. Spacing Rule (간격 규칙)
모든 여백과 간격은 커스터마이징된 토큰 번호를 사용합니다.
- **Padding/Margin**: `p-1` (2px), `p-2` (4px), `p-3` (8px), `p-4` (10px), `p-5` (12px), `p-6` (16px), ...
- **Gap**: `gap-1` (2px), `gap-2` (4px), `gap-3` (8px), `gap-4` (12px), `gap-5` (16px), ...
- **Height**: `h-1` (8px), `h-2` (16px), `h-3` (20px), `h-4` (24px), `h-5` (32px), `h-6` (40px), ...
- *항상 `tailwind.config.js`를 확인하여 해당 숫자가 어떤 CSS 변수와 매핑되는지 대조하십시오.*

## 3. Figma Mapping (피그마 속성 변환)
피그마의 레이아웃 속성을 리액트/테일윈드로 변환할 때 다음 규칙을 고수합니다.
- **Hug (콘텐츠에 맞춤)**: 
  - 고정 `w-*`, `h-*` 클래스 사용 금지.
  - 패딩(`p-*`)과 `w-fit`, `h-fit`을 사용하여 콘텐츠가 스스로 크기를 결정하게 합니다.
- **Fill (컨테이너 채우기)**:
  - `w-full`, `h-full` 또는 상황에 따라 `flex-1`을 사용하여 가용 공간을 가득 채웁니다.
- **Fixed (고정 크기)**:
  - 오직 사전에 정의된 높이 토큰 (`h-7`, `h-8` 등)이나 디자인 시스템상 명시된 고정 규격만 사용합니다.

## 4. Component Style (컴포넌트 작성 방식)
- **Naming**: 모든 컴포넌트는 반드시 **PascalCase**로 명명합니다. (예: `BadgeStatus`, `TabAtomic`)
- **Styling**: 스타일링은 인라인 스타일이나 외부 CSS를 지양하고, **테일윈드 유틸리티 클래스**로만 작성합니다.
- **Conditionals**: 복잡한 스타일 분기는 `classNames` 라이브러리를 활용하여 가독성을 높입니다.

---
**약속**: 앞으로 모든 코드를 작성할 때 이 `instructions.md` 파일을 최우선 순위로 참고하며, 자의적인 판단을 일절 배제하겠습니다.
